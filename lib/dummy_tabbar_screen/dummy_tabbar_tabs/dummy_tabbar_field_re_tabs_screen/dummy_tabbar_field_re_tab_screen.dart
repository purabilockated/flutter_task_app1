import 'package:flutter/material.dart';

class DummyTabbarFieldReTabScreen extends StatelessWidget {
  const DummyTabbarFieldReTabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text(''),
        ],
      ),
    );
  }
}
